var vowels = {
    a : null,
    e : null,
    i : null,
    o : null,
    u : null,
}

if ( element in vowels ) {}

//////////////////

var vowels = ['a', 'e', 'i', 'o', 'u']

if ( vowels.indexOf(element) >= 0 ){
    
}

//////////////////////

var obj = { a : 10, b : 20 };

for ( var key in obj ) {
    console.log(obj[key])
}